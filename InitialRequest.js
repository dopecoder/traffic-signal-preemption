

var InitialRequest = function(){
    this.
    this.locations = new Array();
    this.location_size;
    this.traffic_signals;
}

InitialRequest.prototype.update = function(){
    //update the variables
    //check if enough data is recieved 
    //if enough data is recieved then stop and build PreemptionRequest object
    //queue the object to QueueManager
    //send a response to the phone its successful
}

InitialRequest.prototype.sort = function(location){
    var temp = new Array();
    var smallest = location[0];
    for(var i=1; i<location.length; i++){
        
    }
}